apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- namespace.yaml
- rbac.yaml
- deployment.yaml
- service.yaml
- pdb.yaml
# networkpolicy.yaml is intentionally opt-in because API-server routing differs by CNI/platform.
